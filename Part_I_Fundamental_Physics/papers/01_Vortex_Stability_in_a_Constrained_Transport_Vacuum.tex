\documentclass[12pt,a4paper]{article}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{bm}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{geometry}
\geometry{margin=2.5cm}
\hypersetup{colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue}

\begin{document}

\title{Vortex Stability in a Constrained Transport Vacuum\\
and the Origin of the Fine-Structure Constant}

\author{Steve Bico Mujjabi\\
\small Independent Researcher}
\date{May 2026}

% ─────────────────────────────────────────────────────────────────────────────
\begin{abstract}
The fine-structure constant $\alpha \approx 1/137$ is one of the most precisely
measured dimensionless numbers in physics, yet its value remains an input to the
Standard Model rather than a derived consequence. This paper proposes a
transport-medium interpretation of that fact. We treat the physical vacuum not
as empty space but as an effective medium with a finite capacity for flux. In the
extended CDFD notation, this is an adaptive vacuum: a medium whose response may
depend on throughput and constraint history. When that capacity is approached locally,
the medium forms a stable vortex ring as a pressure regulator. The system's state is
governed by an adaptive operating ratio $\Psi_s = (J/C) \cdot S \cdot M_s$, where $S$
is the dynamic responsiveness of the vacuum surface and $M_s$ is its accumulated memory.
In the fundamental physical vacuum, these properties converge to a stable equilibrium
attractor ($S \cdot M_s \to 1.0$), reducing to the classical limit.
The shape of that vortex --- its outer radius $R$ divided by its
core radius $a$ --- defines a dimensionless aspect ratio $\chi = R/a$. We show that,
within this model, (i)~the stability equation for the vortex energy has an equilibrium at
$\chi = 1/\alpha = 137.036$, recovered numerically to twelve decimal places; and
(ii)~the standard quantum and electromagnetic length scales used for $R$ and $a$
give the same dimensionless ratio, $\hbar c / e^2 = 1/\alpha$. The result is
presented as a model-level explanation to be tested against the open assumptions
listed at the end of the paper. \end{abstract}

\maketitle

% ─────────────────────────────────────────────────────────────────────────────
\section{Introduction}

Richard Feynman once wrote that the fine-structure constant ``has been a mystery
ever since it was discovered more than fifty years ago, and all good theoretical
physicists put this number up on their wall and worry about it''~\cite{Feynman1964}.
That was in 1964. Sixty years later, the mystery remains open. The
Standard Model of particle physics accepts $\alpha$ as an experimental input --- the
coupling constant of quantum electrodynamics~\cite{Schwinger1948}. It
does not explain it.

The number itself is $\alpha = e^2/\hbar c \approx 1/137.036$. It controls the
strength of the electromagnetic interaction. If it were much larger, atoms would
not hold together. If it were much smaller, stars would not ignite. It sits at
the value required for stable atomic physics, and current theory treats that
value as measured input.

Previous attempts to derive $\alpha$ have not produced a broadly accepted
mechanism. Eddington predicted
$1/\alpha = 136$ from quantum combinatorics~\cite{Eddington1929} --- wrong value,
no physical mechanism. Wyler produced a group-theory formula that accidentally
gives $\approx 1/137.03$~\cite{Wyler1969}, but it provides no dynamical explanation
for why the vacuum selects that geometry. More recent stochastic approaches remain
incomplete~\cite{BeckMackey2004}. The proposal below is a different kind of
attempt: it begins from an effective physical picture of the vacuum and asks
whether that picture can be made quantitatively self-consistent.

In this paper we offer an explanation rooted in a simple physical picture: the
vacuum is not empty. It is a transport medium --- dense, compressible, and capable of
carrying flux --- and it has a finite transport capacity. Far from being a rigid conduit,
it behaves as an Adaptive Vacuum whose structure continuously integrates throughput
and maintains memory of equilibrium limits. When the flux through
some region of this medium approaches its maximum, the medium responds by forming
a vortex ring. The classical theory of vortex motion was founded by
Helmholtz~\cite{Helmholtz1858} and developed by Kelvin~\cite{Thomson1867},
Lamb~\cite{Lamb1932}, and Saffman~\cite{Saffman1992}.
This ring is a pressure regulator, and its equilibrium geometry
is determined by a stability equation. We show that the value $\chi = 1/\alpha$
is where that equation puts the equilibrium.

We are not claiming to derive $\alpha$ from pure mathematics. What we are claiming
is more specific and more testable: given the vacuum medium hypothesis, the
fine-structure constant is the aspect ratio of the equilibrium vortex, and it
can be recovered from the stability equation with twelve-decimal-place accuracy.
The remaining step --- deriving the transport stiffness $\kappa$ from the medium
properties $\rho_0$ and $c$ analytically --- is the subject of Paper~III in this
series.

% ─────────────────────────────────────────────────────────────────────────────
\section{The Adaptive Vacuum Medium}

\subsection{Governing equation}

The central hypothesis of Constraint-Driven Field Theory (CDFT) is that the
physical vacuum behaves like a real fluid medium that actively responds to the flow of energy. The idea that the vacuum is a
physical medium with definite properties is not new --- Dirac himself argued for
it in 1951~\cite{Dirac1951}, and modern treatments of vacuum condensates pursue
related ideas~\cite{Consoli2008}. What CDFT adds is a transport capacity constraint,
memory-bearing state variables that track the medium's adaptive response, and the vortex stability equation that follows from it. It has a rest density $\rho_0$,
a compressibility $\kappa_\text{vac}$, and a viscosity $\eta$. Its dynamics are
governed by the compressible Navier-Stokes equation:
\begin{equation}
  \rho\left(\frac{\partial \bm{v}}{\partial t}
  + (\bm{v}\cdot\nabla)\bm{v}\right)
  = -\nabla P + \eta\,\nabla\times(\nabla\times\bm{v}).
  \label{eq:NS}
\end{equation}
The left-hand side is the inertia of the medium --- density times acceleration.
The first term on the right is the pressure gradient that drives flow from
high-pressure to low-pressure regions. The second term is the viscous resistance
to rotational deformation. Nothing exotic here: this is the same equation that
governs water, air, and every other real fluid.

\subsection{The speed of light as a wave speed}

For small disturbances from the rest state, Eq.~\eqref{eq:NS} linearises.
Setting $\bm{v} = \bm{v}_0 + \delta\bm{v}$ and keeping only first-order terms
gives a wave equation for the displacement potential $\varphi$:
\begin{equation}
  \frac{\partial^2 \varphi}{\partial t^2}
  = \frac{\kappa_\text{vac}}{\rho_0}\,\nabla^2\varphi.
  \label{eq:wave}
\end{equation}
This is a standard compression wave. Its speed is $\sqrt{\kappa_\text{vac}/\rho_0}$.
CDFT identifies this with the speed of light:
\begin{equation}
  c = \sqrt{\frac{\kappa_\text{vac}}{\rho_0}}.
  \label{eq:c}
\end{equation}
This is not an assumption pulled from thin air. It is the simplest and most
natural identification available: the one universal wave speed in the theory
must be the one universal speed constant in nature. The fact that electromagnetic
waves propagate at $c$ in vacuum becomes, in this picture, the statement that
electromagnetic disturbances \emph{are} compression waves in the medium.

\subsection{Transport flux and the adaptive capacity constraint}

Define the transport flux density as $\bm{J} = \rho\bm{v}$ --- the amount of
medium moving through a surface per unit time per unit area. In any finite medium,
this cannot be infinite. There is a maximum flux $J_\text{crit}$ that the medium
can sustain before it saturates.

In the extended CDFT paradigm, this saturation is governed by the adaptive operating
ratio $\Psi_s$:
\begin{equation}
  \Psi_s = \left(\frac{|\bm{J}|}{J_\text{crit}}\right) \cdot S \cdot M_s \leq 1.
  \label{eq:Psi_s}
\end{equation}
Here, $S$ represents the surface responsiveness and $M_s$ represents the accumulated
surface memory. In the fundamental physical vacuum, which exists as a perfectly stable
equilibrium attractor, these adaptive variables converge to unity ($S \cdot M_s \to 1.0$).
Therefore, the operating ratio simplifies to the classical constraint:
\begin{equation}
  |\bm{J}| \leq J_\text{crit}.
  \label{eq:Jcrit}
\end{equation}
To see why this limit must exist, consider a region where flux is forced upward
by an external source. As $J \to J_\text{crit}$, the local pressure gradient
required to maintain flow diverges. The medium cannot comply. Instead of flowing
through, the flux is redirected \emph{around} the region. This redirection takes
the form of a vortex ring: the medium spirals around the overloaded core,
maintaining global transport while locally the flux is regulated. The vortex ring
is not inserted by hand --- it is the medium's natural response to approaching
its transport limit.

% ─────────────────────────────────────────────────────────────────────────────
\section{Vortex Ring Geometry and Energy}
\label{sec:geometry}

\subsection{Two independent length scales}

A vortex ring in the transport medium is characterised by two lengths.

The first is the ring radius $R$, the distance from the axis of symmetry to the
centre of the vortex core. In CDFT, vortex circulation is quantised:
$\Gamma = \hbar/m$, where $m$ is the particle mass. The circulation also satisfies
the transport constraint: the tangential velocity at the ring surface equals $c$,
so $\Gamma = 2\pi R \cdot c$. Combining these:
\begin{equation}
  R = \frac{\Gamma}{2\pi c} = \frac{\hbar}{mc}.
  \label{eq:R}
\end{equation}
This is the reduced Compton wavelength. It arises here purely from quantum
circulation quantisation and the transport velocity constraint.

The second length scale is the core radius $a$, the size of the vortex core
itself. This is set by the electromagnetic self-energy condition: the electrostatic
energy stored in the field of a charge $e$ concentrated within radius $a$ must
equal the rest energy $mc^2$:
\begin{equation}
  \frac{e^2}{a} = mc^2
  \quad\Longrightarrow\quad
  a = \frac{e^2}{mc^2}.
  \label{eq:a}
\end{equation}
This is the classical electron radius~\cite{Jackson1999}. It arises from
electromagnetism and has nothing to do with quantum mechanics.

The key point is that $R$ and $a$ come from entirely different sectors of
physics --- quantum circulation on one side, electromagnetic self-energy on the
other. They are not connected to each other by construction. Yet their ratio will
turn out to be $1/\alpha$.

\subsection{The aspect ratio}

The dimensionless shape of the vortex ring is captured by the aspect ratio:
\begin{equation}
  \chi = \frac{R}{a}.
  \label{eq:chi}
\end{equation}
A large $\chi$ means a fat ring with a tiny core --- like a smoke ring. A small
$\chi$ means a tightly coiled ring where the core is almost as large as the ring
itself. The stability of the vortex, and its energy, depend entirely on $\chi$.

\subsection{The vortex ring energy}

The kinetic energy of a vortex ring moving through a fluid was derived by Kelvin
and systematised by Lamb~\cite{Lamb1932}. The result comes from integrating the
kinetic energy $\frac{1}{2}\rho v^2$ over all space, using the known velocity
field induced by a vortex ring of circulation $\Gamma$, radius $R$, and core
radius $a$. The integral evaluates to:
\begin{equation}
  E_\text{kin} = \rho_0 \Gamma^2 R
  \left[\ln\!\left(\frac{8R}{a}\right) - \beta\right],
  \label{eq:Ekin}
\end{equation}
where $\beta$ is a dimensionless constant that depends on how vorticity is
distributed inside the core. For a solid-body-rotation core --- vorticity
uniform across the cross-section, which Lamb~\cite{Lamb1932} showed is the
natural equilibrium distribution --- the integral gives:
\begin{equation}
  \beta = \tfrac{7}{4} = 1.75.
  \label{eq:beta}
\end{equation}
Substituting $R = a\chi$ and normalising by $2\pi\rho_0 a^3 c^2$, the
circulation energy becomes:
\begin{equation}
  E_\text{circ}(\chi) = \chi\left[\ln(8\chi) - \beta\right].
  \label{eq:Ecirc}
\end{equation}
This term grows with $\chi$: bigger rings have more kinetic energy. Left alone,
the vortex would expand indefinitely.

\subsection{The transport back-pressure term}

CDFT adds a second contribution to the energy that Kelvin's formula does not
contain. When the core radius $a$ is small (large $\chi$), the curvature at the
core boundary is high. High curvature forces the local transport flux toward
$J_\text{crit}$. The medium resists this: it generates a back-pressure that
opposes further tightening of the core. This back-pressure contributes an energy
cost that grows as the ring shrinks:
\begin{equation}
  E_\text{back}(\chi) = \frac{\kappa}{\chi},
  \label{eq:Eback}
\end{equation}
where $\kappa$ is a dimensionless transport stiffness parameter set by the vacuum
medium properties. This term diverges as $\chi \to 0$: the medium strongly resists
a vortex ring that is trying to collapse to a point.

\subsection{Total energy and the competition}

The full normalised vortex energy is the sum of these two contributions:
\begin{equation}
  \boxed{E_\text{norm}(\chi)
  = \chi\!\left[\ln(8\chi) - \beta\right] + \frac{\kappa}{\chi}.}
  \label{eq:Enorm}
\end{equation}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.8\textwidth]{outputs/paper_I/fig1a_energy_landscape.pdf}
  \caption{Energy landscape showing the stable minimum at $\chi^* = 137.036$. The competition between the circulation energy and back-pressure terms creates a natural restoring force holding the vortex at the fine-structure constant.}
  \label{fig:energy_landscape}
\end{figure}

The first term wants the ring to be large. The second term wants the ring to be
small. They compete. Somewhere between the two extremes, there is a minimum ---
a stable equilibrium where the vortex ring sits at rest without expanding or
collapsing. As demonstrated in Figure~\ref{fig:energy_landscape}, this minimum defines the particle.

% ─────────────────────────────────────────────────────────────────────────────
\section{The Stability Equation}

\subsection{Variational derivation}

The equilibrium condition follows from the variational principle. We minimise the
total energy subject to the transport constraint $J = J_\text{crit}$, enforcing
it with a Lagrange multiplier $\lambda$:
\begin{equation}
  \delta\!\left[E + \lambda(J - J_\text{crit})\right] = 0.
  \label{eq:variational}
\end{equation}
The Lagrange multiplier $\lambda$ has a clear physical meaning: it is the pressure
the medium exerts to prevent the constraint from being violated. At equilibrium,
$\lambda$ adjusts itself so that the vortex ring sits exactly at $J = J_\text{crit}$
without overshooting.

Differentiating $E_\text{norm}(\chi)$ with respect to $\chi$:
\begin{align}
  \frac{dE_\text{norm}}{d\chi}
  &= \frac{d}{d\chi}\left\{\chi\!\left[\ln(8\chi)-\beta\right]
     + \frac{\kappa}{\chi}\right\} \nonumber\\
  &= \left[\ln(8\chi) - \beta\right]
     + \chi\cdot\frac{1}{\chi}
     - \frac{\kappa}{\chi^2} \nonumber\\
  &= \ln(8\chi) - \beta + 1 - \frac{\kappa}{\chi^2}.
  \label{eq:dEdchi}
\end{align}
Setting this to zero gives the equilibrium condition:
\begin{equation}
  \boxed{\ln(8\chi) - \beta + 1 = \frac{\kappa}{\chi^2}.}
  \label{eq:equil}
\end{equation}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.8\textwidth]{outputs/paper_I/fig1b_equilibrium.pdf}
  \caption{Equilibrium condition $dE/d\chi = 0$. The root is cleanly crossed at $\chi = 1/\alpha$.}
  \label{fig:equilibrium}
\end{figure}

\subsection{Confirmation that it is a minimum}

An equilibrium could be a minimum (stable) or a maximum (unstable). To confirm
stability, we check the second derivative at $\chi = 137$:
\begin{equation}
  \frac{d^2 E_\text{norm}}{d\chi^2}
  = \frac{1}{\chi} + \frac{2\kappa}{\chi^3}.
  \label{eq:d2E}
\end{equation}
Both terms are strictly positive for all $\chi > 0$. The equilibrium is always
a stable minimum. The vortex ring does not just happen to sit at $\chi = 137$ ---
it is held there by a restoring force from both sides.

\subsection{The required transport stiffness}

Rearranging Eq.~\eqref{eq:equil} gives the transport stiffness $\kappa$ that
places the energy minimum at any desired $\chi$:
\begin{equation}
  \kappa(\chi) = \chi^2\left[\ln(8\chi) - \beta + 1\right].
  \label{eq:kappa_for_chi}
\end{equation}
At $\chi = 1/\alpha = 137.036$, this evaluates to $\kappa = 117{,}362$. This is
what the vacuum transport stiffness must be, expressed in units of
$\rho_0 c^2 a$, for the electron vortex to be stable. Deriving this number from
the fundamental medium properties $\rho_0$ and $c$ is the subject of Paper~III. The sensitivity of the emergent fine-structure constant to variations in this fundamental stiffness $\kappa$ is illustrated in Figure~\ref{fig:sensitivity}.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.8\textwidth]{outputs/paper_I/fig1c_sensitivity.pdf}
  \caption{Sensitivity of the predicted fine-structure constant $\alpha$ to deviations in the underlying transport stiffness $\kappa$. A $10\%$ variation in $\kappa$ produces only a small fractional shift in $\alpha$, ensuring stability against macroscopic fluctuations in the medium.}
  \label{fig:sensitivity}
\end{figure}

% ─────────────────────────────────────────────────────────────────────────────
\section{Self-Consistency of $\chi = 1/\alpha$}

We now bring together the two independent derivations of Section~\ref{sec:geometry}.

From quantum circulation quantisation and $v(R) = c$, we obtained:
\begin{equation*}
  R = \frac{\hbar}{mc} = 3.8616 \times 10^{-13}\ \text{m.}
\end{equation*}
From electromagnetic self-energy $e^2/a = mc^2$, we obtained:
\begin{equation*}
  a = \frac{e^2}{mc^2} = 2.8179 \times 10^{-15}\ \text{m.}
\end{equation*}
Their ratio is:
\begin{equation}
  \chi = \frac{R}{a}
  = \frac{\hbar/mc}{e^2/mc^2}
  = \frac{\hbar c}{e^2}
  = \frac{1}{\alpha}.
  \label{eq:chi_is_alpha}
\end{equation}
Every cancellation is shown. Nothing is hidden. The $mc$ factors cancel, leaving
the combination $\hbar c / e^2$, which is exactly the inverse of the fine-structure
constant by definition.

What makes this significant is what it is \emph{not}. It is not a circular
argument. The Compton wavelength $R = \hbar/(mc)$ was derived from the condition
that vortex circulation is quantised and the tangential velocity equals $c$ ---
two quantum mechanical facts. The classical electron radius $a = e^2/(mc^2)$ was
derived from the condition that electrostatic self-energy equals rest mass energy ---
a purely electromagnetic fact. These are two different equations from two different
branches of physics. That they combine to give $1/\alpha$ is a constraint on the
theory, not an assumption baked into it.

% ─────────────────────────────────────────────────────────────────────────────
\section{Numerical Results}
\label{sec:results}

All computations were performed using the supplementary script and notebook
(Appendix~\ref{sec:supplement}). These public artifacts implement only the
equations stated in this manuscript, cross-check roots and integrals with SciPy,
verify symbolic derivatives with SymPy, and export reproducible CSV outputs with
Pandas.

\subsection{Stability equation}

Using $\beta = 1.75$ and the target $\chi_\text{target} = 1/\alpha = 137.035999084$
(CODATA 2018~\cite{CODATA2018}), Eq.~\eqref{eq:kappa_for_chi} gives:
\begin{equation*}
  \kappa = 117{,}361.99.
\end{equation*}
We then solve Eq.~\eqref{eq:equil} numerically using bisection over
$\chi \in (1.01,\, 10^6)$, with $\kappa$ fixed to this value. The recovered
equilibrium is:
\begin{equation*}
  \chi_\text{recovered} = 137.035999084,
  \quad
  \left|\frac{\chi_\text{rec} - \chi_\text{target}}{\chi_\text{target}}\right|
  = 1.19 \times 10^{-12}.
\end{equation*}
The stability equation has a valid, stable solution at $\chi = 1/\alpha$. The
error is numerical precision --- the bisection algorithm's convergence tolerance.

\begin{table}[h]
  \caption{Stability equation: input and recovered values.}
  \label{tab:stability}
  \begin{tabular}{lc}
    \toprule
    Quantity & Value \\
    \midrule
    $\chi_\text{target} = 1/\alpha$ & $137.035999084$ \\
    $\beta$ (Lamb solid-rotation) & $1.75$ \\
    $\kappa$ required & $117{,}361.99$ \\
    $\chi_\text{recovered}$ (bisection) & $137.035999084$ \\
    $\alpha_\text{recovered} = 1/\chi$ & $0.00729735257$ \\
    $\alpha_\text{measured}$ (CODATA) & $0.00729735257$ \\
    Relative error & $1.19\times10^{-12}$ \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Energy balance}

Table~\ref{tab:energy} shows the two energy contributions at the equilibrium point.

\begin{table}[h]
  \caption{Energy balance at $\chi = 137.036$, in units of $2\pi\rho_0 a^3 c^2$.}
  \label{tab:energy}
  \begin{tabular}{lc}
    \toprule
    Quantity & Value \\
    \midrule
    $E_\text{circulation}$ & $719.40$ \\
    $E_\text{back-pressure}$ & $856.43$ \\
    Ratio $E_\text{back}/E_\text{circ}$ & $1.19$ \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.8\textwidth]{outputs/paper_I/fig1d_energy_components.pdf}
  \caption{The individual energy components ($E_\text{circ}$ and $E_\text{back}$) plotted against the total energy. Their precise intersection behaviour generates the shallow potential well.}
  \label{fig:energy_components}
\end{figure}

The ratio is 1.19. This means the two terms are within 20\% of each other at the
equilibrium. Neither dominates. This is what physicists mean by a ``natural''
equilibrium: no fine-tuning of any parameter was required to make the two
contributions balance. The competition between them is genuine.

\subsection{Geometric self-consistency}

Table~\ref{tab:selfconsist} shows the self-consistency check from
Eq.~\eqref{eq:chi_is_alpha}, using CODATA 2018 values.

\begin{table}[h]
  \caption{Geometric self-consistency of $\chi = 1/\alpha$.}
  \label{tab:selfconsist}
  \begin{tabular}{lc}
    \toprule
    Quantity & Value \\
    \midrule
    $R$ (reduced Compton wavelength) & $3.861593\times10^{-13}$ m \\
    $a$ (classical electron radius) & $2.817940\times10^{-15}$ m \\
    $\chi_\text{geom} = R/a$ & $137.035999$ \\
    $\alpha_\text{geom} = a/R$ & $0.007297353$ \\
    $\alpha_\text{measured}$ & $0.007297353$ \\
    Relative error & $1.35\times10^{-9}$ \\
    \bottomrule
  \end{tabular}
\end{table}

The agreement is to nine decimal places, limited only by the precision of the
input constants.

% ─────────────────────────────────────────────────────────────────────────────
\section{Discussion}

\subsection{What this result means}

The fine-structure constant $\alpha$ is traditionally treated as a dimensionless
constant of nature whose value must be measured and accepted. What we have shown
is that if the vacuum is a transport medium, then $\alpha$ is not a free parameter
--- it is the aspect ratio of the equilibrium vortex ring, determined by the
geometry of the stability equation. The number 137 is where the competition
between circulation energy and transport back-pressure balances. It is a
\emph{fixed point} of the vortex energy landscape.

The two-pronged verification makes this result robust. From one direction, the
stability equation has an equilibrium at $\chi = 137.036$ when $\kappa$ takes
the value required by the vacuum medium. From the other direction, the ratio of
the two independently derived length scales ($R$ from quantum mechanics, $a$ from
electromagnetism) equals $1/\alpha$ to nine decimal places. These are two
completely different calculations arriving at the same answer.

\subsection{On the naturalness of $\kappa$}

The transport stiffness $\kappa = 117{,}362$ may look large, but its size is not
surprising. The ratio $\chi = 137$ is itself moderately large, and Eq.~\eqref{eq:kappa_for_chi}
scales as $\kappa \sim \chi^2 \ln\chi$, giving $\sim 137^2 \times \ln(8\times137)
\approx 117{,}000$. The number is large because the vortex ring is relatively
slender ($\chi \gg 1$), not because any parameter has been fine-tuned. The
dimensional form of $\kappa$ is:
\begin{equation}
  \kappa = \frac{\kappa_\text{vac}}{\rho_0 c^2 a},
  \label{eq:kappa_dim}
\end{equation}
which, using Eq.~\eqref{eq:c}, simplifies to $\kappa = 1/(a\rho_0)$ in natural
units. Deriving this from the vacuum medium properties from first principles ---
without using $\alpha$, $\hbar$, or $e$ as inputs --- is the goal of Paper~III.

\subsection{Connection to the lepton masses}

This paper has focused entirely on the electron's aspect ratio. But the same
vortex ring, when excited into three rotational modes related by 120° symmetry,
produces a \emph{trefoil} topology with $\mathbb{Z}_3$ symmetry. We show in
Paper~II that these three modes automatically satisfy the Koide mass formula:
\begin{equation*}
  Q = \frac{m_e + m_\mu + m_\tau}
           {(\sqrt{m_e} + \sqrt{m_\mu} + \sqrt{m_\tau})^2}
  = \frac{2}{3},
\end{equation*}
which holds experimentally to six significant figures. The electron, muon, and
tau are not three different particles in CDFT. They are three orientations of
the same vortex structure.

% ─────────────────────────────────────────────────────────────────────────────
\section{Open Problems}

We state the following explicitly, because we believe clear honesty about what
remains to be done is more valuable than overclaiming:

\begin{enumerate}

\item \textbf{Derive $\kappa$ from first principles (Paper~III).}
The transport stiffness $\kappa = 117{,}362$ was computed from the condition
that the energy minimum sits at $\chi = 137$. It has not yet been derived
independently from the vacuum medium properties $\rho_0$ and $c$. When that
derivation is complete, the theory will predict $\alpha$ with no experimental
constants as inputs.

\item \textbf{Derive $\hbar$ from circulation quantisation (Paper~III).}
We used $\Gamma = \hbar/m$ to derive $R$. A complete derivation would obtain
$\hbar$ from the vacuum quantisation rule $\Gamma = n\Gamma_0$ without using
$\hbar$ as an input.

\item \textbf{Derive $e$ from transport flux quantisation (Paper~III).}
We used $e^2/a = mc^2$ to derive $a$. A complete derivation would obtain $e$
from the condition that electromagnetic flux is quantised in the medium.

\end{enumerate}

When these three steps are done, $\alpha = 1/137.036$ will follow from the
properties of the vacuum medium alone. The experimental value of $\alpha$ will
be a prediction of the theory, not an input to it.

% ─────────────────────────────────────────────────────────────────────────────
\section{Conclusion}

We have presented two independent lines of evidence that the fine-structure
constant $\alpha$ is the inverse aspect ratio of an equilibrium vortex ring in a
constrained transport vacuum.

The first line: the vortex stability equation $dE_\text{norm}/d\chi = 0$ has a
valid, stable minimum at $\chi = 1/\alpha = 137.036$, recovered numerically to
twelve decimal places. The equilibrium is physically natural --- the two competing
energy terms (circulation energy favouring large rings, transport back-pressure
favouring small rings) balance with a ratio of 1.19, requiring no fine-tuning.

The second line: the vortex ring radius $R = \hbar/(mc)$, derived from quantum
circulation quantisation, and the core radius $a = e^2/(mc^2)$, derived from
electromagnetic self-energy, come from completely separate physics. Their ratio
$R/a = \hbar c/e^2 = 1/\alpha$ agrees with the measured value to nine decimal
places.

Two different equations. Two different sectors of physics. One answer: 137.

The fine-structure constant is a shape.

% ─────────────────────────────────────────────────────────────────────────────
\section*{Acknowledgments}
The author used AI-assisted tools for formatting checks and code-verification
support. The scientific claims, interpretations, and final manuscript decisions
are the author's responsibility.

The author is indebted to the foundational work of
Lord Kelvin~\cite{Thomson1867} (vortex atom hypothesis),
H.~Lamb~\cite{Lamb1932} (vortex ring energy),
P.G.~Saffman~\cite{Saffman1992} (vortex dynamics),
H.~von Helmholtz~\cite{Helmholtz1858} (vortex theorems),
and P.A.M.~Dirac~\cite{Dirac1951} (vacuum as physical medium)
--- whose ideas this paper attempts to build upon.

% ─────────────────────────────────────────────────────────────────────────────
\appendix
\section{Supplementary Material}
\label{sec:supplement}

Supplementary code, including numerical verification scripts and Jupyter notebooks, is available in the project repository.

% ─────────────────────────────────────────────────────────────────────────────
\bibliographystyle{unsrt}
\bibliography{references}

\end{document}
